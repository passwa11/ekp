/**
 * 常量、API统一管理处
 */
define(function(require, exports, module) {
	var zonelang = require('lang!sys-zone');
	module.exports = {

		event : {
			// 菜单选中变化事件
			MENU_CHANGED : 'menu.changed',

			ORG_MENU_CHANGED : 'org.menu.changed',
			
			FINDER_MENU_CHANGED : 'finder.menu.changed',
			
			FINDER_MENU_DRAWED : 'finder.menu.drawed',

			CHILD_MIDDLE_CHANGED : 'child.middle.changed',

			OUTER_DOM_FINISHED : 'outer.dom.finished',

			SEARCH_CHANGED : 'search.changed',

			SEARCH_CLEARED : 'search.cleared'
		},

		image : {},

		commonlang : {
			fzr : zonelang['fuzeren'],
			xsjg : zonelang['xiashujiagou'],
			rightContentHint : zonelang['zoneAddress.pageTips'],
			addButton:zonelang['zoneAddress.addButton']
		}
	};
});