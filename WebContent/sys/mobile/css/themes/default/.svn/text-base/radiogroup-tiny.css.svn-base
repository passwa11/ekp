/*@newMui开头，竖向布局开始*/
.newMui.muiFormEleWrap .muiFormEleTip~.muiRadioGroupWrap.muiRadioGroupNormalWrap{
	/*注：此处外框容器margin-top负值是为了抵消首行子元素选项的margin-top
	  避免与实际表单中的td布局的padding-top叠加了整体高度（该值的设定应与子元素的margin-top正负抵消）*/
	margin-top: -1.8rem;
}
.newMui.muiFormEleWrap .muiRadioGroupWrap.muiRadioGroupBlockWrap{
	/*注：此处外框容器margin-top负值是为了抵消首行子元素选项的margin-top
	  避免与实际表单中的td布局的padding-top叠加了整体高度（该值的设定应与子元素的margin-top正负抵消）*/
	margin-top: -1.6rem;	
}
.newMui.muiFormEleWrap .muiRadioGroupWrap:before{
	/*注：此:before伪类的作用是避免子元素的margin-top溢出父元素*/
    content: "";
    display: table;
 }
/*@newMui开头，竖向布局结束*/

/*@oldMui开头，横向布局开始*/

.oldMui.muiFormEleWrap .muiRadioGroupWrap.muiRadioGroupNormalWrap{
	/*注：此处外框容器margin-top负值是为了抵消首行子元素选项的margin-top
	  避免与实际表单中的td布局的padding-top叠加了整体高度（该值的设定应与子元素的margin-top正负抵消）*/
	margin-top: -1.8rem;
}
.oldMui.muiFormEleWrap .muiRadioGroupWrap.muiRadioGroupBlockWrap{
	/*注：此处外框容器margin-top负值是为了抵消首行子元素选项的margin-top
	  避免与实际表单中的td布局的padding-top叠加了整体高度（该值的设定应与子元素的margin-top正负抵消）*/
	margin-top: -1.6rem;	
}
.oldMui.muiFormEleWrap .muiRadioGroupWrap:before{
	/*注：此:before伪类的作用是避免子元素的margin-top溢出父元素*/
    content: "";
    display: table;
 }
 
.muiRadioItem {
	position: relative;
	display: inline-block;
	padding-left: 2.3rem;
	padding-right: 1.4rem;
}
.muiRadioGroupWrap>.muiRadioItem:not(:last-child){
	margin-right: .9rem;
}
.muiFormSelect .muiRadioGroupWrap>.muiRadioItem:not(:last-child){
	margin-right: 0rem;
}

.muiRadioGroupWrap.blockRadio>.muiRadioItem {
	display: block;
}
.muiResponseCard .muiRadioGroupWrap.blockRadio>.muiRadioItem{
  margin: 0;
  padding: 13px 0;
}
.muiRadioCircle {
	width: 1.6rem;
	height: 1.6rem;
	position: absolute;
    left: 0;
    top: .4rem;
	border: 1px solid #d5d5d5;
	border-radius:50%;
	text-align: center;
	margin-right: 0.6rem;
	box-sizing: border-box;
}
.muiFormStatusView .muiRadioCircle,
.muiFormStatusReadOnly .muiRadioCircle{
    background-color: #F2F2F2;
    border: 1px solid #C1C1C1;
}
.muiFormStatusView .muiRadioCircle.active  {
	background-color:#fff;
}

.muiFormStatusReadOnly .muiRadioCircle.active:before{
    color: #C1C1C1;
}


.muiRadioGroupWrap.blockRadio .muiRadioCircle {
	width: 1.6rem;
	height: 1.6rem;
}
.muiRadioCircle.disabled:before,
.muiRadioCircle:before {
	opacity: 0;
}
.muiRadioCircle.disabled {
	background-color: #f1f1f1;
}
.muiRadioCircle.checked:before,
.muiRadioCircle.active:before {
	display: block;
	font-size: 1.6rem;
	line-height: 1.6rem;
	font-weight: 900;
	border: none;
}
.blockRadio .muiRadioCircle.checked:before,
.blockRadio .muiRadioCircle.active:before {
	font-size: 1.6rem;
}
.muiRadioCircle.checked,
.muiRadioCircle.active {
	border: none;
}
.muiRadioCircle.active:before {
	color: #4285f4;
	transition: all .3s;
    opacity: 1;
}
.muiRadioCircle.checked:before {
	color: #d9d9d9;
}

.muiRadioGroupPopWarp {
	position: fixed;
	bottom: 0;
	left: 0;
	right: 0;
	border-top: 1px solid #d3dce6;
}


.muiRadioGroupPopList {
	position: relative;
	z-index: 11;
	list-style-type: none;
}
.muiRadioGroupPopMask {
	position: fixed;
	top: 0;
	left: 0;
	width: 100%;
	height: 100%;
	z-index: 10;
}

@-webkit-keyframes zoomIn {
  from {
    opacity: 0;
    -webkit-transform: scale3d(.3, .3, .3);
    transform: scale3d(.3, .3, .3);
  }

  50% {
    opacity: 1;
  }
}
@keyframes zoomIn {
  from {
    opacity: 0;
    -webkit-transform: scale3d(.3, .3, .3);
    transform: scale3d(.3, .3, .3);
  }

  50% {
    opacity: 1;
  }
}


/* oldMui 过渡样式兼容 Starts */
.oldMui .muiResponse .muiRadioGroupWrap {
/* 	padding: 0 1rem; */
}
.oldMui .muiRadioGroupWrap .muiRadioCircle {
	width: 1.6rem;
	height: 1.6rem;
}
.oldMui .muiRadioCircle.checked:before,
.oldMui .muiRadioCircle.active:before {
	font-size: 1.5rem;
}

/* oldMui 过渡样式兼容 Ends */
.muiResponseCard .muiRadioGroupWrap.blockRadio>.muiRadioItem{
   padding-left: 2.2rem;
}
.muiResponseCard .newMui .muiRadioCircle{
	position:absolute;
	left:0;
	top: 0.25rem;
}
.muiResponseCard .muiRadioGroupWrap>.muiRadioItem{
	padding-left: 2.3rem;
	white-space: normal;
	word-wrap: break-word;
	word-break: normal;
	max-width: 100%;
	box-sizing: border-box;
}
/*@oldMui开头，横向布局结束*/



/** 上下结构   控制【标准】单选框 *号 必填标识字符的显示位置 **/
.newMui.muiFormRadioNormal .muiFormRequiredShow{
	top: 3.3rem;
}

/** 左右结构   控制【标准】单选框 *号 必填标识字符的显示位置 **/
.oldMui.muiFormRadioNormal .muiFormRequiredShow{
	top: 2rem;
}

/** 上下结构   控制【块状】单选框 *号 必填标识字符的显示位置 **/
.newMui.muiFormRadioBlock .muiFormRequiredShow{
	top: 4.4rem;
}

/** 左右结构   控制【块状】单选框 *号 必填标识字符的显示位置 **/
.oldMui.muiFormRadioBlock .muiFormRequiredShow{
	top: 2.9rem;
}

/** 上下结构   控制【表格】单选框 *号 必填标识字符的显示位置 **/
.newMui.muiFormRadioTable .muiFormRequiredShow{
	top: 4.4rem;
}

/** 左右结构   控制【表格】单选框 *号 必填标识字符的显示位置 **/
.oldMui.muiFormRadioTable .muiFormRequiredShow{
	top: 1.4rem;
}
/** #133616 移动端流程审批文本重叠 **/
.detailNode .oldMui.muiFormEleWrap  .muiRadioGroupWrap.muiRadioGroupNormalWrap{
	margin-top: -0.8rem;
}