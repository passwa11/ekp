html {
	background: #F5F6FB;
}

.tableGap{
	height: 1.5rem;
	background: #F5F6FB;
	margin: 0 -1.25rem;
}

.propertyGap {
    height: 3rem;
    background: #F5F6FB;
    margin: 0 -1.25rem;
    padding-left: 0.5rem;    
    line-height: 3rem;
    box-sizing: border-box;
    font-size: 1.3rem;
}

/* 查看页面 */
.muiSysOrgDeptTitle {
	width: 100%;
	height: 5rem;
}

.editBtn {
	background: #fff;
	color: var(--muiFontColorInfo);
	height: 4.7rem;
	font-size: var(--muiFontSizeLA);
	line-height: 3.7rem;
	text-align: center;
	margin: 1rem;
}

.muiSysOrgDeptTitle .fdName {
	line-height: 5rem;
	font-weight: bold;
	float: left;
	margin-right: 1.5rem;
	font-size: var(--muiFontSizeXL);
	color: var(--muiFontColorInfo);
	max-width: 75%;
	overflow:hidden;
	text-overflow:ellipsis;
	white-space:nowrap;
}

.muiSysOrgDeptTitle .tag {
	background: var(--muiFontColor);
	padding: .2rem .4rem;
	color: #fff;
	font-size: var(--muiFontSizeSS);
	float: left;
	border-radius: 2px;
	margin-top: 1.7rem;
	max-width: 50%;
	overflow:hidden;
	text-overflow:ellipsis;
	white-space:nowrap;
}

/* 编辑页面 */
.pageBox{
	margin: 0 1.25rem;
	margin-bottom: 5rem;
}
#authCheckBox{
	display: none;
	margin-top: 2.5rem;
	margin-left: 1.8rem;
}

.sysOrgDept {
	height: 5rem;
    line-height: 5rem;
    text-align: center;
	font-size: var(--muiFontSizeLA);
}

.invalidatedNone {
	height: 14rem;
	background: #F5F6FB;
}

.sysOrgDept.save {
	background: #fff;
	position: fixed;
	bottom: 0;
	width: 100%;
	height: 7rem;
	z-index: 10;
}

.sysOrgDept.save>div {
	border-radius: 1rem;
	background: var(--muiFontColor);
	margin: 1rem;
	color: #fff;
}

#addressOther .muiFormEleTip {
	display: none;
}

.sysOrgDeptRangeGap {
	height: 5rem;
}