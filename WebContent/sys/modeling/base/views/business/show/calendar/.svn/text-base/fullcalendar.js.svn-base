/**
 * 日历组件
 */
define(function(require, exports, module) {
	
	var $=require("resource/js/jquery-ui/jquery.ui");
	var calendar = require("lui/calendar");
	var topic = require("lui/topic");
	var Fixed=require("lui/fixed").Fixed;
	var env=require('lui/util/env');
	var lang = require('lang!sys-ui');
	//日历视图
	var ModelingFullCalendar = calendar.FullCalendarMode.extend({
		init : function($super,_config){
			$super(_config);
		},
	});
	exports.ModelingFullCalendar = ModelingFullCalendar;
	
});
