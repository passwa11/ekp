define([
  "dojo/_base/declare",
  "dijit/_WidgetBase"
], function(declare,WidgetBase) {
  return declare("sys.modeling.main.resources.js.mobile.listView.ViewTab", [WidgetBase], {

    buildRendering: function() {
      this.inherited(arguments);
      this.fixedItem = this;
      //内置
      if(this.buildOtherItems)
        this.buildOtherItems(true);
      //自定义

    },
    startup:function() {
      this.inherited(arguments);
    }

  });
});
