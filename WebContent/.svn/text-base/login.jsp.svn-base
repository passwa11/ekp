<%@ page language="java" contentType="text/html; charset=UTF-8" pageEncoding="UTF-8"%>

<%
	/**
	 * 
	 * 登录页模板灵活配置：在后台配置中（门户引擎 > 门户维护 > 登录页），可以自定义配置登录页模板，配置后不需要重启服务可即使生效。
	 * 如果需要修改登录页的逻辑，请修改下面对应的登录页面，此页面不需要修改
	 * 
	 */

	// 需要跳转的URL
	String url = com.landray.kmss.sys.profile.util.LoginTemplateUtil.getLoginUrl();
	// 页面跳转
	request.getRequestDispatcher(url).forward(request, response);
%>


