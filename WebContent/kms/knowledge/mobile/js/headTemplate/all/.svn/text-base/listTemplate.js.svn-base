define(['mui/createUtils'], function (createUtils) {
    var h = createUtils.createTemplate;
    var all = h('ul', {
        className: 'kmsKnowledgeIndexList',
        dojoType: 'mui/list/HashJsonStoreList',
        dojoMixins: 'mui/list/ComplexLItemListMixin'
    });
    return [all].join(' ');
});


