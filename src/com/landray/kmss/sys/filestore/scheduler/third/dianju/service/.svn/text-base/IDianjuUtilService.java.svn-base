package com.landray.kmss.sys.filestore.scheduler.third.dianju.service;

/**
 * 点聚third/dianju模块工具类服务接口<br/>
 * 因为本模块不能直接关联点聚模块，但因历史代码位置原因，现需要在本模块调用点聚模块工具类方法。<br/>
 * 由此接口解耦。<br/>
 */
public interface IDianjuUtilService {
    /**
     * 第三方到本系统下载附件的地址
     * @param fdId
     * @return
     */
    public String getFilePath(String fdId);
}
