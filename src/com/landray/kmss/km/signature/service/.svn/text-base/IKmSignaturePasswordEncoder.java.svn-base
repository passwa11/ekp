package com.landray.kmss.km.signature.service;

public interface IKmSignaturePasswordEncoder {
	/**
	 * 对密码进行加密
	 * 
	 * @param password
	 * @return
	 */
	public abstract String encodePassword(String password);
}